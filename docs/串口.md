# stty

查看串口设备配置

```shell
stty -F /dev/ttyUSB0 -a
```
# minicom

```shell
minicom -D /dev/ttyUSB0 -b 115200 -o -w
```
-o 不发送 Modem 初始化命令, 不创建锁文件
-w 自动换行(防止截断过长行, 或需要翻页)

# screen

```shell
screen /dev/ttyUSB0 115200
```

# 虚拟端口

```shell
socat -d -d \
  pty,raw,echo=0,link=/tmp/VCOM1,mode=666 \
  pty,raw,echo=0,link=/tmp/VCOM2,mode=666
```
