# 插入数据

```sql
INSERT INTO tb_name (name, age) VALUES ('John', 30);
```

# 更新数据

```sql
UPDATE tablename SET age = 31 WHERE name = 'John';
```

# 删除数据

```sql
DELETE FROM tablename WHERE name = 'John';
```
