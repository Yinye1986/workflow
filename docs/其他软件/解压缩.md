# .gz
```shell
gzip -dk xxx.gz
gunzip -k xxx.gz
```
+ -k: 保留源文件

# .tar.gz
```shell
tar -xzvf xxx.tar.gz
```
+ -x: 解压
+ -z: 使用 gzip 解压
+ -v: 显示解压过程
+ -f: 指定文件名

# .rar
```shell
unrar x xxx.rar # 保留目录结构
unrar e xxx.rar # 不保留目录结构
```

# .7z
```shell
7z a -t7z -mx=9 Archive.7z folder1 file1 flie2
```
