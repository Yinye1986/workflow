# 安装

```shell
# 可选 LSP Server: rust-analyzer
sudo pacman -S rust-up
# 换源
export RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static
# 使用 rustup 安装工具链
rustup install stable
# 更新
rustup update
```

# 项目管理(cargo)

```shell
# 创建新项目
cargo new proj
# 运行
cargo run
```
